class CabModel{
  String id, car_type, car_image, status;

  CabModel(this.id, this.car_type, this.car_image, this.status);
}
class modelModel{
  String id,car_type_id,car_model;

  modelModel(this.id, this.car_type_id, this.car_model);
}